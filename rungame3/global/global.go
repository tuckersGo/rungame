package global

const (
	// ScreenWidth width of screen
	ScreenWidth = 320
	// ScreenHeight height of screen
	ScreenHeight = 240
	// StartSceneText text of startscene
	StartSceneText = "Press Any Button"
	// FrameWidth width of each frame animation
	FrameWidth = 32
	// FrameHeight height of each frame animation
	FrameHeight = 32
	// RunningX x pos of first Running animation frame
	RunningX = 0
	// RunningY y pos of first Running animation frame
	RunningY = 32
	// RunningFrames frame count of running animation
	RunningFrames = 8
	// RunningAnimSpeed update frame count for each running animation frame
	RunningAnimSpeed = 5
	// IdleX x pos of first idle animation frame
	IdleX = 0
	// IdleY y pos of first idle animation frame
	IdleY = 0
	// IdleFrames frame count of idle animation
	IdleFrames = 5
	// IdleAnimSpeed update frame count for each idle animation frame
	IdleAnimSpeed = 12
	// BGScrollSpeed background image scroll speed
	BGScrollSpeed = 2
)
